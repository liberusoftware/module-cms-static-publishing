# Static Publishing

The domain module owns manifest builds, incremental and preview builds, diagnostics, invalidation, rollback, and provider-neutral deployment adapters. Builds retain invalid route diagnostics while publishing only normalized absolute paths.
